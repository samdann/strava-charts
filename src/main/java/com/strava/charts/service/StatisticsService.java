package com.strava.charts.service;

import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;

@Service
@Slf4j
public class StatisticsService {

     /**
      * @param activityType type of activity for which the max heart rate is to be
      *                     calculated
      * @return the max heart rate number for the given input
      */
     public int getMaxHearRateByActivtyType(final String activityType) {

     }
}
